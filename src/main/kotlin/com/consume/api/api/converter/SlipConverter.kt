package com.consume.api.converter

import com.consume.api.dto.SlipDTO
import com.consume.api.model.Slip

object SlipConverter {

//    fun toDTO(slip: Slip): SlipDTO =
//        SlipDTO(
//            id = requireNotNull(slip.id),
//            advice = requireNotNull(slip.advice)
//
//        )
}